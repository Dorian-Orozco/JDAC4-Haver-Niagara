@using Microsoft.AspNetCore.Identity
@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager

@* Used for cookies *@
@inject Microsoft.AspNetCore.Http.IHttpContextAccessor HttpContextAccessor

<style>
    .nav-item:hover, .form-nav-item:hover{
    }
    .logout-icon{
        font-size: 20px;
        position: relative;
        top: 5px;
        right: 2px;
    }

    .form-nav-item {
        width: 100%; 
    }

        .form-nav-item button {
            display: block; 
            width: 100%; 
            padding: 0.5rem 1rem; 
        }

    .dropdown-menu {
        min-width: 220px; 
        text-align: left;
    }
        .dropdown-menu li {
            padding: 0 10px;
        }

        .dropdown-menu .nav-link {
            white-space: nowrap; 
            overflow: visible; 
        }

</style>

<ul class="navbar-nav">
    @if (SignInManager.IsSignedIn(User))
    {
        //Store username in cookie
        var userName = HttpContextAccessor.HttpContext.Request.Cookies["userName"] ?? UserManager.GetUserName(User);
        //Comment to help find this file login LOGIN Login - Menu
        //drop down menu
        <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle text-white" href="#" id="navbarIDDropdown" role="button" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                @userName <span class="d-none">- Menu</span><b class="caret"></b>
            </a>
            <ul class="dropdown-menu dropdown-menu-end bg-nav border-secondary text-center" aria-labelledby="navbarIDDropdown">
                <li class="nav-item pb-1">
                    <a class="nav-link text-white" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">Change Security Settings</a>
                </li>
                <li>
                    @* <hr class="dropdown-divider text-white"> *@
                </li>
                <li class="nav-item border-top border-secondary pt-1 pb-1">
                    <a class="nav-link text-white" asp-area="" asp-controller="EmployeeAccount" asp-action="Details">Manage Account</a>
                </li>
                <form class="form-inline form-nav-item border-top border-secondary pt-1 pe-2" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })" id="logoutForm">
                    <button type="submit" class="nav-link btn btn-link text-white" id="logoutButton"><ion-icon class="text-white logout-icon" name="exit-outline"></ion-icon> Logout </button>
                </form>
            </ul>
        </li>
    }
    else
    {
        @*       <li class="nav-item">
    <a class="nav-link text-white" asp-area="Identity" asp-page="/Account/Register">Register</a>
    </li> *@  @* Accounts will be made by admin and any user who just goes on teh website cannot register to be part of the company *@
        <li class="nav-item">
            <a class="nav-link text-white" asp-area="Identity" asp-page="/Account/Login">Login</a>
        </li>
    }
</ul>

<!--Log out modal  https://www.codeply.com/go/eeBNzYEjCe/bootstrap-log-out-modal-->
<div class="modal fade" id="logoutModal" tabindex="-1" aria-labelledby="logoutModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-sm">
        <div class="modal-content">
            <div class="modal-header">
                <h4 class="modal-title" id="logoutModalLabel">Logout <i class="fa fa-lock"></i></h4>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <i class="fa fa-question-circle"></i> Are you sure you want to log out?
            </div>
            <div class="modal-footer">
                <button type="button" class="nav-link btn btn-link text-white" data-bs-toggle="modal" data-bs-target="#logoutModal"><ion-icon class="text-white logout-icon" name="exit-outline"></ion-icon> Logout </button>
            </div>
        </div>
    </div>
</div>  //work on model 

<script>
    $(document).ready(function () {
        // Handle click event for logout button
        $('#logoutButton').click(function () {
            // Show the logout modal
            $('#logoutModal').modal('show');
        });

        // Handle form submission for logout
        $('#logoutForm').submit(function (e) {
            e.preventDefault(); // Prevent form submission

            // Submit the form (perform logout)
            $(this).submit();
        });
    });

    // document.addEventListener('DOMContentLoaded', function () {
    //     //select the logout button by ID
    //     var logoutButton = document.getElementById('logout-button');

    //     //add click event listener to the logout button
    //     logoutButton.addEventListener('click', function (event) {
    //         //prevent the form from submitting
    //         event.preventDefault();

    //         //display confirmation dialog
    //         var userConfirmed = confirm('Are you sure you want to log out?');

    //         //if the user confirms, submit the form
    //         if (userConfirmed) {
    //             //find the form that contains the logout button and submit it
    //             logoutButton.form.submit();
    //         }
    //     });
    // });
</script>
